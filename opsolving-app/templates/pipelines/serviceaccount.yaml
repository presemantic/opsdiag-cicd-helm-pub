{{/* Copyright Opsolving. All Rights Reserved. SPDX-License-Identifier: APACHE-2.0 */}}
{{- if .Values.pipelines.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "opsolving-app.serviceAccountName" (dict "root" . "component" "pipelines") }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels:
    {{- include "opsolving-app.podLabels" (dict "root" . "component" "pipelines") | nindent 4 }}
  {{- if .Values.pipelines.serviceAccount.annotations }}
  annotations:
    {{- with .Values.pipelines.serviceAccount.annotations }}
    {{- include "common.tplvalues.render" (dict "value" . "context" $) | nindent 4 }}
    {{- end }}
  {{- end }}
automountServiceAccountToken: {{ .Values.pipelines.serviceAccount.automountServiceAccountToken }}
{{- end }}
