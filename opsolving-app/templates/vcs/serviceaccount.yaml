{{/* Copyright Opsolving. All Rights Reserved. SPDX-License-Identifier: APACHE-2.0 */}}
{{- if .Values.vcs.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "opsolving-app.serviceAccountName" (dict "root" . "component" "vcs") }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels:
    {{- include "opsolving-app.podLabels" (dict "root" . "component" "vcs") | nindent 4 }}
  {{- if .Values.vcs.serviceAccount.annotations }}
  annotations:
    {{- with .Values.vcs.serviceAccount.annotations }}
    {{- include "common.tplvalues.render" (dict "value" . "context" $) | nindent 4 }}
    {{- end }}
  {{- end }}
automountServiceAccountToken: {{ .Values.vcs.serviceAccount.automountServiceAccountToken }}
{{- end }}
