{{/* Copyright Opsolving. All Rights Reserved. SPDX-License-Identifier: APACHE-2.0 */}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsolving-app.componentName" (dict "root" . "component" "api") }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels:
    {{- include "opsolving-app.podLabels" (dict "root" . "component" "api") | nindent 4 }}
  {{- with .Values.api.service.annotations }}
  annotations:
    {{- include "common.tplvalues.render" (dict "value" . "context" $) | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.api.service.type }}
  {{- with .Values.api.service.clusterIP }}
  clusterIP: {{ . }}
  {{- end }}
  ports:
    - name: http
      port: {{ .Values.api.service.ports.http }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "opsolving-app.selectorLabels" (dict "root" . "component" "api") | nindent 4 }}
