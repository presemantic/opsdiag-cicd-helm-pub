{{/* Copyright OpsDiag. All Rights Reserved. SPDX-License-Identifier: APACHE-2.0 */}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "opsdiag-app.componentName" (dict "root" . "component" "pipelines") }}
  namespace: {{ include "common.names.namespace" . | quote }}
  labels:
    {{- include "opsdiag-app.podLabels" (dict "root" . "component" "pipelines") | nindent 4 }}
  {{- with .Values.pipelines.service.annotations }}
  annotations:
    {{- include "common.tplvalues.render" (dict "value" . "context" $) | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.pipelines.service.type }}
  {{- with .Values.pipelines.service.clusterIP }}
  clusterIP: {{ . }}
  {{- end }}
  ports:
    - name: http
      port: {{ .Values.pipelines.service.ports.http }}
      targetPort: http
      protocol: TCP
  selector:
    {{- include "opsdiag-app.selectorLabels" (dict "root" . "component" "pipelines") | nindent 4 }}
