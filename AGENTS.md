# OpsDiag Charts Instructions

## Purpose

This project is `opsdiag-cicd-helm-pub`, the public Helm chart repository for customer-deployable OpsDiag artifacts. It publishes one unified `opsdiag-app` chart for the complete application stack and one independent `opsdiag-app-connector` chart for customer-side relay connectivity.

## Structure

- [`opsdiag-app/`](./opsdiag-app/) deploys App API, App Agent, App Front, App Scheduler, App Pipelines, the thin App MCP Proxy, App VCS, the mandatory official Apache APISIX gateway, and two independent official Bitnami PostgreSQL dependencies for the main App store and Pipeline Tables. The two PostgreSQL aliases are enabled by default, own separate Services and persistent volumes, and may be disabled independently when the matching external database URL is supplied. The chart also owns optional single-backend Kubernetes Ingress, OpenShift Route, Istio Gateway, and Istio VirtualService resources that expose only APISIX.

App chart `0.1.172` is the embedded PostgreSQL release. It vendors Bitnami PostgreSQL chart `18.7.11` twice as `postgresql` and `tables-postgresql`, pins the PostgreSQL 18.4.0 multi-platform image digest, enables self-signed TLS, and creates independent standalone primary Services and PVCs for App state and Pipeline Tables. App API, migrations, and Pipelines derive their database URLs from the matching alias unless an explicit URL is supplied; disabling an alias requires that external URL. This release replaces the two production Percona App database Applications; Control PostgreSQL is unchanged and no existing App data is migrated.

App chart `0.1.171` removes the unified chart's generic `extraDeploy` escape hatch and its `extra-list.yaml` renderer. Every object deployed by `opsdiag-app` must now have an explicit chart-owned template and contract; arbitrary additional Kubernetes objects belong in the caller's deployment layer. Component images and runtime behavior are unchanged, and this release requires no database migration.

App chart `0.1.170` is the readable standalone APISIX manifest release. The parent ConfigMap now renders the complete `apisix.yaml` route and upstream objects directly in `apisix-standalone.yaml` instead of constructing nested temporary dictionaries and lists before serializing them. Standalone topology and routing validation lives in a separate non-rendering helper, preserving all safety checks and the exact generated routing contract without obscuring the Kubernetes object. Component images and runtime behavior are unchanged, and this release requires no database migration.

App chart `0.1.169` is the MCP Self instruction-discovery clarification release. It pins MCP Proxy `2026-08-09.06-45-16.6941f6c` by its verified registry digest and states directly in the `instructions_write` tool that Flow references require `body.kind=flow`, Reviewer references require `body.kind=review`, the kinds are not interchangeable, and complete instruction fields should be preserved on update. Other component images remain unchanged and this release requires no database migration.

App chart `0.1.168` is the complete MCP Self Global Administration and multi-tenant context release. It pins App API `2026-08-09.05-53-28.b6d386d` and MCP Proxy `2026-08-09.05-53-28.e285ce9` by their verified registry digests. `workspace_context` returns the signed-in user, selected OAuth tenant, all tenants available to that user, and explicit workspace, tenant-management, and Global Administration permissions. The fixed Self catalog retains every selected-tenant UI mutation and adds separately role-gated tenant, global-user, and tenant-membership read/write/delete tools; every Global Administration dispatch reloads and checks the caller's current `global_admin` role before invoking the existing Admin API, which checks that role again. Selecting another tenant still requires a new OAuth authorization and token, so workspace operations never choose or override tenant scope in tool arguments. Configuration-owned default model profiles remain immutable, secrets remain write-only, and this release requires no database migration.

App chart `0.1.167` is the complete MCP Self tenant-management release. It pins App API `2026-08-09.04-53-13.f310dfb` and MCP Proxy `2026-08-09.04-53-32.284b88c` by their verified registry digests. The compile-time Self catalog covers every selected-tenant mutation exposed by the App UI across tenant settings and members, Flows, Pipelines, model profiles, credentials, variables, tables, instructions, Knowledge, Reviewers, scheduling, analyses, and private chats. Configuration-owned `type=default` model profiles are explicitly immutable at discovery and rejected before update/delete dispatch; tenant-created `custom:<uuid>` profiles retain create, update, test, and delete behavior. Global administration and secret readback remain excluded, and this release requires no database migration.

App chart `0.1.166` is the complete MCP documentation catalog release. It pins MCP Proxy `2026-08-09.04-17-33.d2dbe7a` by its verified registry digest. `documentation_index` discovers every canonical documentation article generated by the public documentation build, while `documentation_read` returns an exact catalogued article as complete Markdown and rejects URLs, unlisted paths, traversal, invalid catalogs, and redirects. The human `/docs` portal does not expose the machine transport or internal documentation tools. Other component images remain unchanged, and this release requires no database migration.

App chart `0.1.164` is the complete documentation integration release. It pins Front `2026-08-08.22-53-01.3efaa6c` and MCP Proxy `2026-08-08.22-54-07.d45738e` by their verified registry digests. MCP Self setup and the MCP `documentation_index` now point to the canonical complete `/docs` portal, while bounded machine-readable AI guides remain under `/docs/ai`; the obsolete standalone `/docs/mcp/self` article is not part of the documentation catalog. This release requires no database migration and leaves all other component images unchanged.

App chart `0.1.163` is the concise MCP CLI setup release. It pins Front `2026-08-08.22-15-16.28c366b` by its verified registry digest, stacks Codex and Claude command cards vertically at full width on both MCP Proxy and MCP Self, and removes setup-step prose, protocol badges, OAuth helper copy, and detached security/helper banners so long commands remain directly readable. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.162` is the APISIX ClusterIP Service compatibility hotfix. It keeps the official chart's unconditional `externalTrafficPolicy` field empty because Kubernetes permits that policy only on externally accessible Service types, and it rejects future non-empty overrides at Helm render time. All routes, component images, and runtime configuration remain unchanged; no database migration is required.

App chart `0.1.161` is the central standalone APISIX gateway release. It adds official APISIX chart `2.16.0` and image `3.17.0` as a mandatory two-replica Deployment behind one ClusterIP Service, renders all product paths from one parent-owned `apisix.yaml`, and disables etcd, the Admin and Control APIs, the APISIX Ingress Controller and CRDs, Gateway API resources, the dashboard, subchart Ingress, metrics Service, and stream listeners. Kubernetes Ingress, OpenShift Route, and Istio now carry only hostname/TLS plus one `/` route to APISIX; App API, Front, MCP Proxy, and VCS Services remain internal. The packaged chart carries the Apache 2.0 license and APISIX NOTICE, component images are unchanged, and no database migration is required.

App chart `0.1.160` is the MCP Self Table contract release. It pins API `2026-08-08.20-22-37.1c234d8` and MCP Proxy `2026-08-08.21-09-58.08a4eb8` by their verified registry digests. OAuth authorization-server discovery advertises only OIDC identity/session scopes, leaving `mcp:self` and `mcp:tools` exclusively to their exact protected-resource metadata so Codex automatic setup requests only the selected resource. MCP Self keeps the bounded `documentation_read` tool and now states directly in Table tool descriptions that row values use returned column UUIDs, filters use `filter.<columnUuid>`, and row IDs are numeric. Other component images remain unchanged and this release requires no database migration.

App chart `0.1.158` is the resource-bound OAuth dynamic-client-registration hotfix. It pins API `2026-08-08.19-52-09.d3f752a` by its verified registry digest and ensures dynamically registered CLI clients receive only identity/session defaults from registration; the exact `mcp:self` or `mcp:tools` scope is then requested from the selected protected resource metadata. This prevents a `/mcp/self` login from accidentally requesting the proxy scope while preserving strict audience and cross-resource scope rejection. Other component images remain unchanged and this release requires no database migration.

App chart `0.1.157` is the tenant-scoped MCP Self management release. It pins Front `2026-08-08.19-25-24.6ad9795`, API `2026-08-08.19-25-24.5056bfa`, and MCP Proxy `2026-08-08.19-25-24.76b6153` by their verified registry digests. `/mcp/self` has a distinct OAuth resource and `mcp:self` scope, forwards only fixed typed operations to the internal App API bridge, and reloads the caller's current tenant membership and role on every operation; it never exposes global tenant or user administration. Front adds `MCP -> Self` setup for Codex and Claude, while the machine documentation discovery tool points to the public tenant-safe guides. Other component images remain unchanged and this release requires no database migration.

App chart `0.1.156` is the public MCP Proxy path and CLI-setup release. It pins Front `2026-08-08.18-13-59.ef50d4f`, API `2026-08-08.18-13-59.98f4c7c`, and MCP Proxy `2026-08-08.18-13-59.1379e0a`. The exact protected resource, token audience, proxy route, challenge, and path-specific protected-resource metadata move from `/mcp` to `/mcp/proxy`; `/mcp/oauth/*` authorization-server routes remain unchanged and the legacy `/mcp` proxy endpoint is removed. Front adds the tenant-scoped `MCP -> Proxy` workspace route with copyable Codex and Claude CLI commands derived from the current application origin. Other component images remain unchanged and this release requires no database migration.

App chart `0.1.155` is the tenant-isolated MCP and Agent-access release. It pins Agent `2026-08-08.17-15-51.f2c2725`, API `2026-08-08.17-15-53.54a17cf`, and MCP Proxy `2026-08-08.17-15-52.7991329`. Every enabled Agent access requires a canonical `tenants` list; App API places the consent-selected tenant ID and slug in OAuth/OIDC tokens and propagates that slug into flow/provider metadata, the proxy maintains an independent catalog for each verified tenant, and Agent filters both discovery and execution. OIDC publishes `/mcp/oauth/userinfo`. Other component images remain unchanged and this release requires no database migration.

App chart `0.1.154` is the inline Flow Custom-instruction release. It pins Front `2026-08-08.16-22-12.13d986f`, removes the secondary Custom-instruction drawer, and renders the instruction textarea plus explicit save action immediately below `Custom` through all height remaining in the existing node Settings drawer. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.153` is the dynamic Flow link-anchor release. It pins Front `2026-08-08.15-55-35.dad18de`, gives every saved Flow edge its own visible source and target `LINK` balls, moves those balls around each node toward their connected counterpart, spreads multiple nearby endpoints apart, and recomputes endpoint placement together with edge curves while nodes move. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.152` is the Flow and Pipeline editing-workflow release. It pins Front `2026-08-08.15-34-55.969dbf7`, lets Delete/Backspace remove complete Flow canvas selections, opens the Flow Custom instruction editor directly from the source choice, and keeps node settings closed when users add or drop new Flow or Pipeline nodes so placement and connection can happen before configuration. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.151` is the Flow connector-anchor release. It pins Front `2026-08-08.11-34-57.6eaee2b` and makes saved edges plus live connection previews run from the exact center of the source `LINK` ball to the exact center of the target `LINK` ball instead of using either node badge center. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.150` is the visible Flow node-label release. It pins Front `2026-08-08.11-13-15.dbd778b`, raises full node-name label bars above the circular badge faces, and renders their white text after the badge and semantic/provider icon so node names cannot be obscured. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.149` is the unified saved-Pipeline preview release. It pins Front `2026-08-08.01-47-07.820e304` and makes the saved Pipeline manager preview reuse the editor's node cards, semantic icons, handles, route labels, and dynamic node heights with the same capped fit scale. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.148` is the Pipeline multi-selection release. It pins Front `2026-08-08.01-30-58.eb33310`, adds Shift-drag marquee selection on empty Pipeline canvas, keeps Shift/Ctrl/Cmd-selected nodes movable and removable as one set, and prevents multi-selection from opening the single-node settings drawer. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.147` is the canvas-control cleanup release. It pins Front `2026-08-08.01-17-14.1bf26fc`, removes the visible zoom-in, zoom-out, Fit, Reset, and zoom-percentage controls from both Flow and Pipeline builders, and retains wheel, trackpad, touch, pointer navigation, plus automatic opening layout. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.146` is the dynamic Switch routing release. It pins Front `2026-08-08.01-00-20.3566b1b` and Pipelines `2026-08-08.01-00-33.baa452a`: multi-output Pipeline cards grow by 24 pixels for every port beyond the initial pair, and all handles, edges, previews, graph bounds, and collision checks use the computed height. Switch still activates only the first matching rule or fallback, but now forwards the previous node input unchanged instead of exposing its internal routing envelope. Other component images remain unchanged and this release requires no database migration.

App chart `0.1.145` is the redesigned Custom Execution icon release. It pins Front `2026-08-08.00-44-24.c4863dc`, removes the system-emoji grid and free-form glyph field, and replaces them with large labeled Code, Web, Automation, Transform, Inspect, Validate, Integrate, and Package vector tiles reused by Pipeline cards. Legacy saved emoji/text values map onto the new vectors. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.144` is the landing-style Flow node release. It pins Front `2026-08-08.00-35-34.096a76d`, replaces the empty colored Flow cores with black circular badge faces, provider-colored rings, and white provider logos matching the landing, and uses the existing semantic white icons for management and unknown agents. The animated edges, ground rings, and `LINK` connector remain unchanged. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.143` is the grouped Pipeline palette release. It pins Front `2026-08-08.00-16-40.46200d5`, removes the instructional footer below the Pipeline node catalog, and groups the unchanged node set under `Triggers`, `Control`, `Network`, `Processing`, and `Delivery` headings matching the Flow palette hierarchy. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.142` is the full-width builder-palette correction. It pins Front `2026-08-07.23-51-25.4a1f2af`, removes the Flow `draggable agents` count, visually hides the Flow and Pipeline palette scrollbars while preserving scrolling, and removes scrollbar padding so palette rows keep the complete `18rem` width. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.141` is the corrected Flow canvas and builder-palette release. It pins Front `2026-08-07.23-41-25.6cd8968`, restores the original layered circular Three.js Flow nodes, widens both Flow and Pipeline palettes to `18rem` with unwrapped `text-sm` labels and internal scrolling, and makes the `DEFAULT MODEL` caption presentation-only so the model list opens only from the selector field. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.140` is the unified Flow/Pipeline node-card and model-placement release. It pins Front `2026-08-07.22-49-40.3ae0649`, replacing Flow text initials and circular canvas models with the same semantic vector-icon family and rectangular card hierarchy used by Pipelines, matching palette row and drag-preview geometry, moving the default Flow Model selector beside `Publish`, removing the toolbar settings toggle, and keeping the selected-node drawer limited to the node override. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.139` is the fullscreen Flow builder alignment release. It pins Front `2026-08-07.22-24-29.a113668`, placing Flow editing above the complete Config viewport with the same right-side red close action, palette width, icon field size, and drag-preview geometry as Pipelines. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.138` is the unified Pipeline icon release. It pins Front `2026-08-07.22-10-06.10084fe`, replacing mixed text glyphs with a larger consistent `24x24` vector icon set across the node palette, drag preview, and canvas cards. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.137` is the inline AI Transform prompt release. It pins Front `2026-08-07.21-57-34.02ecfdc`, removes the secondary prompt drawer, and lets the Prompt textarea fill the remaining height of the ordinary node settings drawer. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.136` is the Pipeline AI Transform discoverability release. It pins Front `2026-08-07.21-31-24.636312a`, which presents the existing executable Pipeline `model` node as `AI Transform`, explains its structured previous-node input and `$input.message` output contract, and preserves saved graph compatibility. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.135` is the corrected protected-runtime release. It pins Agent `2026-08-07.13-19-15.18be637`, API `2026-08-07.13-19-19.08ebdcc`, MCP Proxy `2026-08-07.13-19-29.5198530`, Pipelines `2026-08-07.13-19-33.7b4ac5a`, Scheduler `2026-08-07.13-19-36.c01f2df`, and VCS `2026-08-07.13-19-41.0486ea5` to shared license runtime `v0.4.5`. Protected breakpoint probes now read verified function-entry bytes directly and no longer treat denied `/proc/self/mem` access as `I11`; Pipelines exposes explicit `bubblewrap` and `broker` launch modes with no silent fallback, and production GKE selects `broker` because its nodes deny unprivileged user namespaces. Front remains `2026-08-06.22-29-39.22d10ce`; this release requires no database migration.

App chart `0.1.130` is the Pipeline Variable type-label release. It pins Front `2026-08-06.22-29-39.22d10ce`; every Variable list card now shows exactly `Secret` or `Plain`, and the technical `write-only` copy is removed from the list. Backend images remain unchanged and this release requires no database migration.

App chart `0.1.129` is the production-binary hardening release. It pins Pipelines `2026-08-05.00-16-32.d347842`, MCP Proxy `2026-08-05.00-16-40.380fec6`, and VCS `2026-08-05.00-16-36.444ed22`; Pipelines now uses deterministic Garble and removes runtime package managers, while all three releases attest the exact image pulled after push. Front was `2026-08-04.23-13-15.2c455ac`; this release requires no database migration.

App chart `0.1.127` is the Flow/Pipeline template-manager release. It pins Front `2026-08-04.23-00-43.c0e628a`, which gives both manager sidebars the same first row: wide `Create from template` on the left and compact `Reload` on the right. Template selection now opens the shared full-height 80vw drawer instead of occupying the manager list, and Pipelines include Model-free examples for If, Filter, Logic Gate, Switch, and Merge. The backend image pins and values-only `/app/config.yaml` mounts from `0.1.126` remain unchanged; this release requires no database migration.

App chart `0.1.126` is the values-only runtime cleanup release. It pins App API `2026-08-04.04-26-22.95bda87`, Agent `2026-08-04.04-26-22.193f35d`, MCP Proxy `2026-08-04.04-26-22.045013c`, VCS `2026-08-04.04-26-22.1a8c041`, Pipelines `2026-08-04.04-26-22.5dfc8c1`, Scheduler `2026-08-04.04-26-22.08f9516`, and Front `2026-08-04.00-53-52.127db5b`. Backend images contain no repository runtime config or local Compose target; every backend receives runtime YAML only from the chart-owned Secret mounted read-only at `/app/config.yaml`. This requires no database migration.

App chart `0.1.125` pins App API `2026-08-04.02-12-18.4ada783`, Front `2026-08-04.00-53-52.127db5b`, and Pipelines `2026-08-04.00-32-06.fd9363e`. App API upgrades signed instruction bundles idempotently when a newer bundle repeats an unchanged immutable Template version; identity, graph, and prompt-set hash mismatches still fail closed. `Execute now` opens the standard 80vw live drawer and reveals each node's persisted input and terminal output as the worker advances; Flow and Pipeline managers keep one selection-driven Export action below Import, support multi-file import, and round-trip multi-selection bundles. Custom Execution can still return `$stop(output)` to finish its active branch successfully: descendants with no other active input are skipped, so an atomic `insertUnique` duplicate can retain execution evidence without calling Slack or Teams again.

For the current chart, this release summary supersedes older interaction wording retained below: both Flow and Pipeline omit Back arrows and close only through the standard red `X` at the far right of the toolbar. Single-item transfer documents remain compatible, and the versioned bundle formats add multi-item round trips without restoring server metadata.

The current unified App chart source version is `0.1.169` with `appVersion` `2026-08-09`. The `0.1.169`, `0.1.168`, `0.1.167`, `0.1.166`, `0.1.165`, `0.1.164`, `0.1.163`, `0.1.162`, `0.1.161`, `0.1.160`, `0.1.159`, `0.1.158`, `0.1.157`, `0.1.156`, `0.1.155`, `0.1.154`, `0.1.153`, `0.1.152`, `0.1.151`, `0.1.150`, `0.1.149`, `0.1.148`, `0.1.147`, `0.1.146`, `0.1.145`, `0.1.144`, `0.1.143`, `0.1.142`, and `0.1.141` summaries above supersede every older current-version, ingress-routing, MCP-access, Flow-instruction, Flow-link-layout, Flow-editing, Flow-edge, Flow-label, Pipeline-preview, Pipeline-selection, canvas-control, Switch-output, custom-glyph, Flow rectangular-card, and palette-width statement retained below: APISIX is the only public application gateway and sole application-routing authority; the public MCP resources are `/mcp/proxy` and `/mcp/self`, OAuth discovery keeps resource scopes path-specific, MCP Self exposes selected-tenant workspace tools plus independently role-gated Global Administration tools, returns only catalogued canonical documentation articles through its internal documentation tools, and reports every OAuth-visible tenant without allowing a tool call to override token tenant scope; MCP and provider access are tenant-isolated by the consent-selected slug, the human `/docs` portal does not publish internal machine-catalog instructions, the MCP setup surfaces use concise vertically stacked full-width CLI command cards without detached helper banners, Flow Custom instructions edit inline below `Custom` in the existing Settings drawer, every Flow edge owns dynamic per-endpoint `LINK` balls that follow counterpart direction and spread apart, new Flow and Pipeline nodes keep their settings closed until a later explicit interaction, Flow selections support Delete/Backspace, Flow edges and live previews connect the visible `LINK` balls at both ends, Flow name labels render visibly above their badges, the saved Pipeline preview reuses the editor card design and fit cap, Pipeline supports modifier and marquee multi-selection, Flow and Pipeline omit visible camera controls, Switch forwards input unchanged through its selected port, Custom Execution uses the labeled vector picker, FlowCanvas keeps circular Three.js nodes with landing-style badges, and Pipeline keeps dynamically sized rectangular cards.
- [`opsdiag-app-connector/`](./opsdiag-app-connector/) remains an independent chart because the connector is installed separately in customer environments.

The current unified App chart source version is `0.1.140` with `appVersion` `2026-08-07`. It includes the mandatory internal App Pipelines runtime with Node.js 24, a standard HTTPS CA bundle, and a Kubernetes-verifiable numeric non-root UID, authenticated MCP Proxy, OAuth/OIDC discovery and `/mcp/oauth/*` authorization routing, `/mcp` exposure while reserving `/oauth` for App UI login, and the App VCS webhook route. The chart pins compatible App API, Front, and Pipelines releases with current Agent, MCP Proxy, VCS, and Scheduler releases; the executable Pipeline `model` node is presented as `AI Transform`, consumes the previous node output as structured model input, and exposes the reply as `$input.message` while retaining saved graph compatibility. Opening a Pipeline graph overlays the complete browser viewport so the shared Config header, cream padding, and outer frame no longer reduce the editor canvas. The Agentic Flow manager exports portable versioned JSON containing only a flow name and graph and imports validated JSON as a new tenant draft without restoring server identity, publication state, tenant metadata, or timestamps. Workspace Tables use PostgreSQL bigint identity row IDs, lazily replace legacy random-text IDs with sequential numbers, and hide immutable row IDs and automatic timestamps from custom-column controls while rendering ID, Created at, and Updated at in the data table; row edits preserve ID and creation time and automatically advance the update time. Their row grid keeps ID compact on the left, user columns flexible in the center, timestamps and actions compact on the right, filters each user column from its header, expands values longer than sixteen characters on demand, and creates rows in the standard wide right-side drawer with vertically stacked rows that align column names left and full-width controls right; successful table mutations refresh in place without detached top banners, while errors remain explicit; duplicate display names keep the inline New form open and render an accessible Name-field conflict instead of exposing the raw API code. Credentials, Variables, and Tables remove redundant helper prose and share the larger Knowledge/Instructions label, control, action, and table typography; `Secret variable` is a compact accessible switch placed immediately beside its label, orange while editable and neutral grey in disabled read-only mode. Front normalizes a rolling-version workspace-table detail that omits an empty `columns` field so creating a zero-column table cannot blank the configuration drawer, while API always serializes an explicit empty `columns` array for zero-column tables. Main-nav Executions places its shared full-width Reload action at the top of the black execution-list panel above the execution count. Front keeps Pipelines, Saved, Scheduled, and Reviews in tenant-level Analyses, moves Executions into a separate Pipelines navigation group after Chats, restores the Pipeline builder entry inside Config, orders Config frames as Agentic, Pipelines, and Resources at the far right, reuses that complete framed header above the standalone tenant Pipelines manager, keeps its outer header geometry fixed while switching between Config and tenant-level Pipelines, and uses the tenant as the sole user-facing resource boundary without group creation, assignment, filtering, badges, or grouped list sections. Flow and Pipeline builders omit their redundant outer title rows and share compact Name/save-state top bars; Pipeline opens current-Pipeline execution history and per-node results in an on-demand full-screen inspector with explicit Reload and no background polling, and double-clicking Custom Execution opens its full-width code editor immediately. Flow replaces text initials and circular canvas models with the same semantic vector icons and rectangular dark cards used by Pipelines across the collapsible palette, drag preview, and Three.js canvas. Flow keeps its draft status, `Save Draft`, `Publish`, and default Model together in that top bar, has no generic settings toggle beside the red close action, and opens an item drawer containing only the selected node override or connection settings; both managers keep confirmation-backed deletion plus explicit reload feedback. Pipeline preview has no duplicate floating Edit action and Undo sits beside `Execute now` in the bottom-centered canvas action group. The Flow and Pipeline manager sidebars omit visible manager titles and shown/total counters; Pipeline retains search, range/modifier multi-selection, bulk deletion, preview, import/export, and the six-node palette with drag-and-drop placement, endpoint-aware bidirectional pointer connection/rewiring, platform undo, infinite pan/zoom, execution node results, Custom-to-Print echo defaults, and modifier-key node multi-selection with multi-node movement. Pipelines derives its dedicated Tables database connection from the App API Tables configuration, and Custom Execution exposes a bounded tenant-scoped `tables` SDK over an inherited-descriptor bridge with `list`, `find`, `insert`, `insertUnique`, `update`, and `delete`; `insertUnique` uses PostgreSQL transaction-scoped advisory locking for cross-replica atomic deduplication by user-selected visible columns while keeping physical identifiers and database credentials outside Node.js. The Custom Execution editor now offers the tenant table and column catalog, generates `find`, `insert`, and `insertUnique` snippets with selectable deduplication columns, supports configurable Custom Execution icons, and renders tenant Variables as a larger click-to-insert list containing only variable names, without values, secret masks, or redundant helper copy; newly created downstream expressions use `{{$input.PATH}}`, while Pipelines keeps `{{$json.PATH}}` compatible for saved graphs. Selected Flow and Pipeline bulk-delete actions use the same solid-red visual state as the remaining managers. Scheduler, Models, Instructions, Reviewers, Knowledge, Credentials, Variables, and Tables use their active Config tab as the only visible page identity, start directly inside the standard surface padding without a detached identity/action spacer, and use matching 22rem black list panels without repeated outer/inner titles, shown/total counters, or resource-list search fields. Every `Delete selected` action now opens the same count-aware confirmation dialog with correct singular or plural resource wording before any request is sent. Every two-pane manager renders one shared action component in the exact order `Reload`, solid-red `New`, optional supporting action, `Delete selected`, and selection state, so the button fonts, heights, colors, divider, and empty-list offset are identical across Agentic and Pipeline resources. Scheduler, Models, Instructions, Reviewers, Knowledge, Credentials, and Variables share the same center-panel interaction: selecting a left card shows the same disabled neutral-grey fields used by editing, one bottom-right red `Edit` makes those fields white and editable, `Save` disables and greys the same fields again, and `New` opens a blank editor there. New and Edit modes begin only after explicit user actions; reload, card selection, clear, bulk deletion, and Config-tab navigation silently discard unfinished editor state. Populated managers restore the last valid selection in read-only mode, while empty managers show a disabled blank template and never auto-open creation or display a center empty-selection message. Restoring an authenticated session preserves the initially requested tenant Config deep link instead of redirecting to the main workspace during interim reactive state changes, and synchronizes the routed manager after loading without auto-opening any New form. They have no list-card edit action, content-panel delete action, or right-side create/edit drawer. Credentials, Variables, and Tables reuse the Knowledge manager's card typography, metadata sizing, selection states, filled empty-list messages, and dashed light empty-content panels. Credentials and Variables retain range/modifier multi-selection, bulk deletion, secret redaction, and immutable variable secrecy; Credentials expose only Slack and MS Teams webhooks, and Variable names are normalized and enforced as uppercase letters, digits, and underscores starting with a letter. Tables uses the separately configured PostgreSQL workspace for tenant display-name tables, typed column add/drop, searchable paginated row reads, and row create/update/delete while physical table and column identifiers stay hidden. API and Pipelines enforce bounded acyclic graphs, semantic five-field cron schedules, bounded execution output, no longer accept HTTP Request nodes, expose variables to Custom Execution through `$vars`, recursively redact exact secret values before persistence or logs, and let ad-hoc `Execute now` runs use a Schedule Trigger as their entry point when no Manual Trigger exists while retaining manual execution attribution. Custom Execution exposes bounded Node.js `fetch`, `process`, CommonJS `require`, dynamic imports, and built-in modules while the Node permission model continues to deny host filesystem, child-process, worker-thread, and native-addon access. Pipeline migration `038_pipeline_runtime.sql` adds queueing, schedule specs, node results, credentials, and variables, `039_pipeline_variable_secrets.sql` adds the write-only variable flag, `040_pipeline_resource_contract.sql` enforces the supported credential destinations and canonical Variable grammar for new writes without deleting legacy rows, and `041_pipeline_workspace_tables.sql` stores tenant table metadata separately from physical workspace data. Enabled App API migrations run as pre-install/pre-upgrade hooks so the required schema exists before new API and Pipelines pods start. The protected backends consume shared license runtime `v0.4.5`; chart version changes do not authorize invented component image tags, and production deployment values must continue to avoid image overrides.

The current independent Connector chart source version is `0.2.11` with `appVersion` `2026-08-07.13-19-25.1b2a443`. It consumes shared license runtime `v0.4.5`, pins the exact attested GAR digest, and retains the complete runtime configuration only in a Kubernetes Secret mounted read-only at `/app/config.yaml`, plus the values-only runtime and overlapping Relay-session behavior. Transient Control transport or 5xx failures retain only an existing still-valid verified grant. Do not replace it with a locally rebuilt binary or an unpublished tag.

- [`.github/workflows/ci.yml`](./.github/workflows/ci.yml) packages both public charts on timestamp release tags and publishes them as OCI artifacts to `europe-west1-docker.pkg.dev/prod-common-cicd/opsdiag-helm-pub`.

## Chart Design

Use the Bitnami-style component hierarchy in `values.yaml`. App API, App Agent, App Front, App Scheduler, App Pipelines, App MCP Proxy, and App VCS are mandatory parts of the unified release and must always render; do not expose per-component `enabled` switches. Each component owns its replicas, image, service account, pod configuration, service, probes, resources, scheduling, disruption budget, and runtime config. Component image registry, repository, tag, digest, and pull policy defaults belong to the chart; production values must not override them. The unified chart `appVersion` is suite metadata, while the exact released image is pinned independently under each component's `image.tag`.

The public `values.yaml` is also the chart's parameter reference. Keep it organized in Bitnami-style `## @section` blocks, document every supported public value with an adjacent `## @param` description, separate major runtime, image, security, networking, resource, probe, scheduling, migration, and exposure groups with whitespace, and include concise commented examples for structured lists such as Ingress rules. Documentation-only formatting must preserve the rendered value tree exactly.

Stable application defaults such as health probes, component runtime behavior, service ports, security contexts, rollout strategies, Control/Relay origins, scheduler wiring, migration retry behavior, PostgreSQL topology and PVC sizes, and the API disruption budget belong in the chart and must not be repeated in environment values. Official component images do not expose Helm `command`, `args`, or `resourcesPreset` overrides; migration uses its fixed binary command and every workload accepts only explicit `resources`. App API always mounts and reads `/app/config.yaml`, and scheduler integration is mandatory rather than feature-gated. The single-replica Agent uses a no-surge rolling replacement (`maxSurge: 0`, `maxUnavailable: 1`) as its chart-owned default. Deployment repositories explicitly own production replica counts and resources together with settings that genuinely vary by environment or customer, such as credentials, licenses, provider targets, model profiles, embedded-database passwords and storage classes or external database endpoints, migration acknowledgements, Go runtime sizing, exposure, and exceptional rollout constraints.

Internal App API, Agent, Scheduler, MCP Proxy, and VCS upstream URLs must be rendered from `opsdiag-app.componentName` and the selected component service port. Never hardcode the default `opsdiag-app-*` release names in runtime environment variables or generated component configuration. The generated URLs must follow the actual Helm release name and support both `nameOverride` and `fullnameOverride`.

Application exposure keeps the application topology private to the chart. The mandatory official APISIX subchart runs only as a two-or-more-replica Deployment with one ClusterIP gateway Service in `standalone` `data_plane` mode, using the parent-generated ConfigMap as its YAML provider. etcd and external etcd, Admin and Control APIs, dashboard UI, APISIX Ingress, APISIX Ingress Controller and CRDs, Gateway API, metrics Service and ServiceMonitor, and TCP/UDP stream listeners must remain disabled. App API, Front, MCP Proxy, and VCS Services must remain ClusterIP.

The generated `apisix.yaml` is the only source of application routing and owns, in descending priority, OAuth protected-resource discovery to MCP Proxy, OAuth/OIDC authorization-server discovery and `/mcp/oauth` to API, `/mcp/proxy` and `/mcp/self` to MCP Proxy, `/api/vcs/hooks` to VCS, `/api` to API, and `/*` to Front. Routing values may reference only the release-aware internal Services for `api`, `front`, `mcp-proxy`, and `vcs`; route IDs must be unique and URIs absolute. Kubernetes Ingress, OpenShift Route, and Istio expose only hostname, TLS, and a single `/` catch-all to the APISIX Service, including additional hosts; they must never duplicate application paths or target component Services directly.

Application templates must remain thin and reuse the Opsolving `common` library from `https://opsolving.github.io/charts/` for names, labels, images, pull secrets, templated values, resources, and affinity helpers. Both public charts vendor the exact `common` archive pinned by their `Chart.lock`; the unified App chart also vendors the exact official APISIX and Bitnami PostgreSQL archives pinned by its lock so a clean checkout can lint and package without dependency downloads. Rebuild an archive only when its lock changes. Every value must be consumed by a template or be an intentional dependency value. Preserve stable application workload and service names during chart refactors.

The App API, App Agent, App Scheduler, App Pipelines, App MCP Proxy, and App VCS runtime YAML is rendered from their component `config` blocks into component-specific Kubernetes Secrets mounted read-only at `/app/config.yaml`; licenses, credentials, webhook key material, JWT material, model secrets, database endpoints, and worker tokens therefore never enter ConfigMaps. App API, its migration hook, and Pipelines must use the same chart-derived main and Tables PostgreSQL URLs. Explicit API database URLs take precedence and are the external-database contract; otherwise the enabled alias requires an inline custom-user password and produces a TLS URL to its release-aware ClusterIP Service. The standalone customer Connector chart also renders its complete config as an Opaque Secret with mode `0440`, never as a ConfigMap, and assigns projected files to `fsGroup: 1000` so only the non-root runtime group can read them. App Pipelines additionally receives a chart-owned size-bounded `/tmp` `emptyDir`; its explicit `bubblewrap` mode isolates the JavaScript broker in namespaces without the Secret mount or main binary, while explicit `broker` mode supports kernels that deny unprivileged user namespaces and relies on the existing non-root, no-new-privileges, read-only-container boundary plus Node permissions. The chart never silently falls back between modes. App Front does not receive a fake runtime config. `global.externalURL` is the single canonical OAuth issuer; the chart renders it into App API and MCP Proxy config and renders all internal App service URLs from release-aware service names.

## Connector Constraints

The connector chart renders `/app/config.yaml` from top-level `config`. Its standard license and Control Edge URL live directly under `config`; relay license, WebSocket URL, `connectTimeoutSeconds`, and `maxFrameBytes` live under `config.relay`. In the unified App chart, Agent, Pipelines, Scheduler, and VCS each own a separate `config.connector` client block with `enabled`, Relay origin, client license, and Control Edge origin; the three newer clients default to the existing Agent Connector license but remain independently overridable. These client licenses authorize transport grants only and do not move provider credentials into Connector. External model/provider/MCP/webhook/Custom Execution fetch traffic uses those clients, while App API/database/service-to-service traffic remains direct. Do not duplicate derived `/api/...` paths, project these values through environment variables, or restore a separate top-level connector section.

The connector must remain compatible with restricted platforms. Do not set fixed `runAsUser` or `runAsGroup` defaults. The chart defaults `fsGroup` to `1000` so the non-root process can read the projected `0440` configuration Secret; clusters that allocate an SCC-specific supplemental group must override that value with an allowed GID. Keep non-root execution, no privilege escalation, a read-only root filesystem, dropped capabilities, and RuntimeDefault seccomp.

## Release Constraints

Chart dependencies are limited to the public Opsolving `common` library and, for the unified App chart, official Apache APISIX plus two aliases of the official Bitnami PostgreSQL chart. APISIX is mandatory; both PostgreSQL aliases are default-enabled but independently replaceable by external URLs. Every dependency must remain pinned by `Chart.lock`; APISIX's optional etcd and ingress-controller dependencies must stay disabled and render no resources. The release workflow must resolve timestamp tags directly from the Git ref, must not depend on `presemantic/actions-helpers` or `GH_ACTIONS_HELPERS_TOKEN`, and must not rewrite component image tags or chart `appVersion` during packaging.

Product documentation belongs in `opsdiag-docs-ai-context`, not this repository. Keep public chart defaults free of tenant credentials, endpoints, licenses, or environment-specific values.

The breaking gateway transport release is unified App chart `0.1.7` plus customer Connector chart `0.2.0`; unified App chart `0.1.8` is the frontend security-header hotfix and `0.1.9` adds continuous encrypted gateway-grant readiness monitoring to the Agent. Public workloads do not expose container command/args, component-specific extra volumes/mounts, or any generic `extraDeploy` escape hatch. `extraEnvVars`, `extraEnvVarsCM`, and `extraEnvVarsSecret` remain supported. Front always renders chart-owned `emptyDir` mounts for Nginx runtime paths and defaults to a read-only root filesystem.

## Pipeline Model Runtime Wiring

The unified chart owns a single private Pipeline worker token at `api.config.pipelines.workerToken`. App API receives it directly, while the Pipelines Secret copies the same value into `pipelines.config.appApi.workerToken` and derives `pipelines.config.appApi.url` from the release-scoped App API Service. Public values must not require users to duplicate the token or hardcode an internal Service name.

Pipeline Model nodes default to a ten-minute `pipelines.config.runtime.modelTimeoutSeconds` and a one-hour `runTimeoutSeconds`; the whole-run bound must never be shorter than one model call. The internal App API call remains direct cluster traffic, while the Agent keeps responsibility for provider transport and model credentials.

When App API migration is enabled, the chart renders a dedicated pre-install/pre-upgrade migration config Secret at hook weight `-1` and mounts it into the migration Job. This keeps the hook on the same desired runtime config as the release even when the normal long-lived App API Secret has not been updated yet; both migration hook resources use the hook-success deletion policy.

## Generic Pipeline Nodes and Editor Viewport Release

Unified App chart `0.1.125` pins App API `2026-08-04.02-12-18.4ada783`, Front `2026-08-04.00-53-52.127db5b`, and Pipelines `2026-08-04.00-32-06.fd9363e`. This supersedes the older current-version, node-catalog, and removed-HTTP wording above: App API accepts a repeated immutable Template version in a newer signed instruction bundle only when its identity, graph, and prompt-set hashes match, fixing v18-to-v19 upgrades without a database migration. Pipelines support If, Logic Gate, Switch, Filter, Merge, Loop Over Items, HTTP Request, and generic outbound Webhook alongside the existing nodes. Branches persist named `sourcePort` values, converging active ports from one parent contribute one downstream input, HTTP/Webhook use only Generic credentials and Connector-aware egress, and Webhook is not an inbound public trigger. API, worker, editor, and documentation enforce the same route-label and HTTP-header bounds. HTTP Request and Webhook both accept GET, POST, PUT, PATCH, DELETE, and HEAD. The shared Config/Pipeline workspace covers the complete viewport; secondary editors remain `80vw` by `100dvh`, Pipeline node forms use their full available width, Message fields show at least ten lines, and node deletion lives on Delete/Backspace or the red hover/focus `X` instead of a settings action. The release needs no database migration; production must keep the migration Job disabled for this rollout.
